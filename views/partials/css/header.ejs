<style>
    header {
        background-color: var(--fdl-background);
        text-align: center;
        -webkit-user-select: none;        
        -moz-user-select: none; 
        -ms-user-select: none; 
        user-select: none;
        z-index: 1;
        position: relative;
    }
    header a {
        text-decoration: none;
        color: currentColor;
        cursor: pointer;
    }
    header #header-img {
        height: 8rem;
        margin: 1rem 0 0;
    }
    header #header-text {
        color: var(--primary-color);
        padding: 0;
        margin: 0;
    }   
    header #header-text:hover {
        color: var(--primary-dark-color);
    }
    @media(max-width: 999px) {
        header #header-text {
            font-size: 1.5rem;
        }   
    } 
    @media(min-width: 1000px) {
        header #header-text {
            font-size: 3rem;
        }   
    }
</style>